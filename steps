
# Navigate to project root
cd access-control-poc

# Create virtual environment
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On macOS/Linux:
source venv/bin/activate

##Install Dependencies

# Install chatbot dependencies
pip install -r chatbot/requirements.txt

# Install website dependencies  
pip install -r website/requirements.txt

test connecction
python init_database.py
python test_setup.py


# Activate virtual environment
source venv/bin/activate  # Windows: venv\Scripts\activate

# Initialize the database with 9 tables
python init_database.py

# Start the applications
cd chatbot && uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload &
cd website && uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload &




manually run requrements
 
# Install basic packages first
pip install fastapi==0.104.1 uvicorn==0.24.0 sqlalchemy==2.0.23 psycopg2-binary==2.9.11

# Install OpenAI and related packages
pip install openai==1.12.0 requests==2.31.0 python-dotenv==1.0.0 jinja2==3.1.2

# Install authentication packages
pip install python-jose[cryptography]==3.3.0 python-multipart==0.0.6

# Try installing pydantic with pre-compiled wheels
pip install pydantic==2.5.0 --only-binary=all

# Install LangChain components
pip install langchain==0.0.346 langchain-community==0.0.10 langchain-core==0.1.0

# Finally, try ChromaDB
pip install chromadb==0.4.15